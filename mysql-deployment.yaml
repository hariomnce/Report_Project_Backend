apiVersion: apps/v1kind: Deploymentmetadata:  name: mysql-deploymentspec:  replicas: 1  selector:  template:    metadata:      labels:        app: mysql    spec:      containers:        - name: mysql          image: mysql          envFrom:            - secretRef:                name: mysql-secret